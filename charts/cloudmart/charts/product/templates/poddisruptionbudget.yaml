apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "product.fullname" . }}-pdb
  namespace: {{ .Values.namespace | default "cloudmart-prod" }}
  labels:
    {{- include "product.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "product.selectorLabels" . | nindent 6 }}
